name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request_target:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  # Build job for pull requests (without deployment)
  build_job:
    if: github.event_name == 'pull_request_target' && github.event.action != 'closed'
    runs-on: ubuntu-latest
    name: Build Job (PR)
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false
          ref: ${{ github.event.pull_request.head.sha }}
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'pnpm'
      
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      
      - name: Build project
        run: pnpm build
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            dist/
            apps/*/dist/
          retention-days: 7

  # DEPLOYMENT DISABLED - Uncomment the jobs below to re-enable deployment
  
  # build_and_deploy_job:
  #   if: github.event_name == 'push' || (github.event_name == 'pull_request_target' && github.event.action != 'closed')
  #   runs-on: ubuntu-latest
  #   name: Build and Deploy Job
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         submodules: true
  #         lfs: false
  #         ref: ${{ github.event.pull_request.head.sha }}
  #     
  #     - name: Setup pnpm
  #       uses: pnpm/action-setup@v2
  #       with:
  #         version: 8
  #     
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20.x'
  #         cache: 'pnpm'
  #     
  #     - name: Install dependencies
  #       run: pnpm install --frozen-lockfile
  #     
  #     - name: Build project
  #       run: pnpm build
  #     
  #     - name: Build And Deploy
  #       id: builddeploy
  #       uses: Azure/static-web-apps-deploy@v1
  #       with:
  #         azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
  #         repo_token: ${{ secrets.GITHUB_TOKEN }}
  #         action: "upload"
  #         app_location: "dist/demo" # App source code path
  #         api_location: "" # Api source code path - optional
  #         output_location: "dist/demo" # Built app content directory
  #         skip_app_build: true # We already built the app in previous step

  # close_pull_request_job:
  #   if: github.event_name == 'pull_request_target' && github.event.action == 'closed'
  #   runs-on: ubuntu-latest
  #   name: Close Pull Request Job
  #   steps:
  #     - name: Close Pull Request
  #       id: closepullrequest
  #       uses: Azure/static-web-apps-deploy@v1
  #       with:
  #         azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
  #         action: "close"